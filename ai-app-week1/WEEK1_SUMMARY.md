# Week1 Summary

## 1. 本周完成了什么
- 创建 Python 虚拟环境
- 建立最小项目结构
- 实现 CLI 程序
- 加入配置读取
- 加入日志输出
- 加入自定义异常
- 加入基础测试
- 支持通过 `python -m app` 运行
- 上面是gpt给我的，我自己学习到了Python相关的语法，如Optional，写法的不同等
- 目前觉得测试不知道是不是需要这么弄，可能大型项目就是需要测试文件。对于文件结构的构建或者文件的分类的思路还很欠缺。

## 2. 本周我真正掌握了什么
- 如何从脚本思维切到项目思维
- 如何拆分入口层和业务层
- 如何写最小测试闭环
- 如何让 CLI 支持退出码和参数注入
- Python的一些基础语法

## 3. 本周踩过的坑
- `pytest` 直接运行不稳定，最终统一改用 `python -m pytest`
- 导入路径和项目根目录容易混
- 改了函数签名后，测试需要同步修改
- python版本不同写法不同

## 4. 当前项目还缺什么
- 还没有 HTTP API
- 还没有请求/响应 schema
- 还没有鉴权
- 还没有数据库

## 5. 下周进入 FastAPI 前，我应该记住什么
- 先复用已有服务层，不要推倒重来
- FastAPI 的 route 本质上也是入口层
- 异常、日志、配置、测试思维会继续复用